# Public sandbox credential used only by the server-side ABDM proxy.
ABDM_API_KEY=replace_with_abdm_api_key

# Cloudflare Zero Trust > Networks > Tunnels > your tunnel > Add a replica.
CLOUDFLARE_TUNNEL_TOKEN=replace_with_cloudflare_tunnel_token
